📋 Специфікація веб-додатку «Shared ToDoLists»
1. Загальний опис

система має дві основні сторінки: редагування і перегляд

сторінка редагування: можна редагувати списки і задачі в них, для кожного списку задач можна отримати посилання за яким його можна переглянути в режимі читання
виконані задачі не показуються в списках задач. задачі помічаються в базі як виконані, не видаляються

сторінка читання: в адресі сторінки передається id списку, згідно цього виводяться всі невиконані задачі, що відносяться до цього списку. порядок виводу задач одниковий з сторінкою редагування

Веб-додаток призначений для створення, редагування та перегляду списків задач.
Додаток мінімалістичний, працює без фронтенд-збірщиків, з фокусом на простоту деплою та підтримки.

2. Функціональні вимоги
2.1 Списки задач
Створення нового списку задач
Перегляд усіх доступних списків
Редагування назви списку
Видалення списку


2.2 Задачі

Додавання задачі до списку
Редагування тексту задачі
Позначення задачі як виконаної
Видалення задачі


має бути реалізоване Автозбереження всіх змін, що користувач вносить. наприклад зміна назв, переміщення задач в списку. технічно після будь-якої зміни на бекенд відправляється запит для внесення відповідної зміни в базу


3.1 UI / Frontend

Alpine.js — для реактивності, переконайся що весь js-код використовує саме інструкції alpine.js
Bulma — для стилізації

Чистий HTML + JS

3.2 
на бекенді має використовуватись python flask
вся серверна логіка має міститись в корні проекту в файлі flask_app.py
всі данні додатку мають зберігатись в файловій базі sqlite

3.3 Принципи


Мінімальний JavaScript, максимум логіки — на сервері

Адаптивна верстка (mobile-first)

3.3 Сторінки
на сторінці редагування є ліва панель, де можна переглядата, редагувати і видаляти списки задач. основна частина сторінки - це перегляд і редагування задач з вибранного списку
поле і кнопка «Створити список» для створення нової задачи в списку
задачі в списку можна переміщувати, збереження позиції задачі відбувається за рахунок поля з типом Numeric, завдяки цьому при переміщенні задачі треба просто перерахувати нове значення її позиції по значенню сусідів цієї задачі
після кліку по спеціальній іконці для списку задач в буфер копіюється посилання на перегляд цього списку задач

Публічний перегляд списку задач для анонімного користувача. 



--


мені треба веб-додаток для створення, редагування і перегляду списків задач.
на фронтенді треба для всіх сторінок використати alpine.js, bulma 
не треба використовувати зборщики на фронтенді
дані мають зберігатись у реляційній базі данних
